import React from 'react'

export default function singlePostCard({ title, publishDate, excerpt, featureImage }) {
    return (
        <div className="single-post-card">
            
        </div>
    )
}
